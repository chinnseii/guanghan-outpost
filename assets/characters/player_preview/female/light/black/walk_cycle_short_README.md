# female_light_black_shorthair_walk

Rows: `down`, `left`, `right`, `up`. Columns: `00` through `05`.

Import with nearest filtering and alpha enabled. Create four looping animations at 8 FPS: `walk_down`, `walk_left`, `walk_right`, `walk_up`. Keep the sprite origin at feet center.
